import './bootstrap';

// Importar Flowbite
import 'flowbite';

// Importar Simple DataTables
import {DataTable} from "simple-datatables"

// Inicializar Simple DataTables
/* document.addEventListener('DOMContentLoaded', () => {
    const datatableElement = document.querySelector('#datatable');
    if (datatableElement) {
        new DataTable(datatableElement);
    }
}); */

/* import 'simple-datatables/src/style.css'; */
